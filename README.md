# 鸡父母平台 · MVP 演示网站（v0.29 · 多页架构 · 17 页）

> **🌐 线上访问（双渠道）**：
> - GitHub Pages（国际）：https://mina2026best.github.io/jfm-platform-demo/
> - EdgeOne Pages（国内直连）：已认领并部署 v0.29（多页版）；国内访问采用预览链接制（3 小时有效、可随时刷新），详见《EdgeOne 上线与认领指引》
>
> GitHub Pages：main 分支根目录，`git push` 后约 1 分钟自动重新发布。
> EdgeOne：腾讯中国站免费计划；项目已认领（归入账号），升级一键更新到同一项目。
> 本地副本仍可双击 `index.html` 离线打开，三端内容一致（单文件零依赖架构）。

## 零、v0.29 更新要点（2026-09-24 · 多页架构改版）

1. **单页拆为 17 个独立页面**：首页 / 资讯 / 日历 / 政策 / 自查 / 学校 / 对比 / 志愿 / 求真 / 社区 / 生活 / 升学豆 / 我的 / 会员 / B端 / 数据来源 / 关于——每页仍单文件自包含、可双击打开。
2. **全站真跳转**：顶栏 / 移动菜单 / 站点地图 / 正文互链全部改为 `.html` 页面链接（导航含 aria-current）。
3. **跨页参数联动**：搜索直达 `news.html?s=关键词`、`schools.html?school=校名` 自动展开；收藏对比 `compare.html?fav=1` 自动恢复。
4. **旧锚点自动重定向**：`index.html#policy` 等旧链接自动跳到对应页面，外链/收藏不失效。
5. **构建系统升级**：build.py 按页面配置装配 17 页；渲染函数全部带元素守卫，跨页安全初始化。

验证：v0.29 **24/24**（17 页加载 / 每页功能 / 跨页流 / 移动端 / JS 零错误）。

## 零、v0.28 更新要点（2026-09-24 · 链接真实化与内容充实）

1. **数据来源表可点击**：9 条来源全部挂官方入口（重庆市统计局 / 市教委 / 阳光高考 / 新华网 / 艾媒 / 北大 CIEFR 等），表内「原文入口 ↗」新窗直达。
2. **核验角标链接化**：页面数字旁的「已核验」角标带「原文 ↗」链接。
3. **资讯扩至 30 条**：新增 4 条带官方来源链接的条目（教委招生通知 / 特招线 / 军校答疑 / 志愿填报市场），资讯条目内「原文链接 ↗」。
4. **搜索索引增强**：全站搜索与资讯搜索现在覆盖正文与来源字段。

验证：v0.28 **94/94**（Playwright 全量回归逐轮通过）。

## 零、v0.27 更新要点（2026-09-24 · 上帝视角审查优化）

1. **打印样式补齐**：倒计时条 / 材料清单表单 / 学校筛选 / 我的统计条等交互控件在打印时自动隐藏，结果区保留。
2. **导出键补全**：`jfm_ann_closed`（公告条关闭状态）纳入导出 JSON，与导入 / 清空键列表对齐。
3. **公告条更新**：文案从 v0.10 旧版更新为 v0.23–v0.26 新功能概览。
4. **搜索占位更新**：从「搜政策 / 学校 / 术语…」扩展为「搜政策 / 学校 / 资讯 / 术语…」。

验证：v0.27 **91/91**（Playwright 全量回归逐轮通过）。

## 零、v0.23–v0.26 更新要点（2026-09-22 晚间 · 体验优化批）

1. **v0.23 数据互通**：本机数据导入恢复（JSON 反向导入，覆盖确认 + 刷新生效）；「我的」页数据统计条（孩子 / 提醒 / 对比收藏 / 本机资讯）。
2. **v0.24 今日提示**：首屏关键节点倒计时条——自动取自升学日历节点，显示「距 XX 还有约 N 天」并直达日历。
3. **v0.25 材料清单**：入学材料清单生成器（学段 × 户籍 × 住房 → 分组清单，复制 / 打印；演示口径标注）。
4. **v0.26 键盘与筛选**：搜索面板 ↑↓ / Enter 键盘导航（高亮 + 直达档案）；学校库公办 / 民办筛选 chips。

验证：v0.23 82/82 · v0.24 84/84 · v0.25 86/86 · v0.26 88/88（Playwright 全量回归逐轮通过）。

## 零、v0.19–v0.22 更新要点（2026-09-22 上午 · 内容与工具扩充）

1. **v0.19 学校档案库扩容**：6 → 12 所（新增南岸 / 江北 / 渝中 / 九龙坡 / 大渡口 / 两江新区 6 校，含 1 所民办样本）；全站搜索直达学校档案（点击直接打开档案弹窗）；陪读租房行情样本同步扩至 5 校。
2. **v0.20 政策库与术语词典**：政策库 +4 条（随迁子女 / 中考体育 / 学生资助 / 民办摇号）；术语词典去重并扩至 18 条（新增位次 / 强基计划 / 军检线 / 征集志愿 / 生源地助学贷款 / 跨区就读）。
3. **v0.21 内容扩充**：家长学堂 8 问答 + 6 经验；求真台 9 条；社区 7 帖；资讯扩容至 26 条（校历 / 流感防护 / 材料季 / 开放日 / 家庭协议 / 家长会等）。
4. **v0.22 陪读成本速算器（新工具）**：片区 × 房型 × 生活档位 × 通勤方式 → 每月开销区间（房租 + 生活 + 通勤），一键复制结果；口径为样本演示。

验证：v0.19 72/72 · v0.20 75/75 · v0.21 78/78 · v0.22 80/80（Playwright 全量回归逐轮通过）。

## 零、v0.15–v0.18 更新要点（2026-09-22 上午 · 第二批四轮自主迭代）

1. **v0.15 深色模式**：跟随系统 / 浅色 / 深色三态切换（本机记忆 `jfm_theme`），首屏防闪烁，全组件深色适配，打印自动保持浅色。
2. **v0.16 资讯快搜 + 阅读工具**：资讯关键词搜索（标题/摘要/来源，命中条数提示）；返回顶部按钮；顶部阅读进度条。
3. **v0.17 键盘与无障碍**：全站键盘焦点可见描边；「?」快捷键帮助弹窗；弹窗 Tab 焦点圈定 + Esc 关闭 + 焦点还原；筛选/开关 aria 状态同步。
4. **v0.18 择校对比增强**：差异标记「●」标出各校存在差异的维度；「仅看差异」一键折叠一致项；深色模式同步适配。

验证：v0.15 60/60 · v0.16 63/63 · v0.17 66/66 · v0.18 69/69（Playwright 全量回归逐轮通过）。

## 零、v0.11–v0.14 更新要点（2026-09-22 凌晨 · 四轮自主迭代）

1. **v0.11**：资讯扩容至 20 条；站点公告条（可关闭、持久化）；搜索快捷键「/」；导航滚动高亮（scrollspy）。
2. **v0.12**：家长学堂（专家问答×5 + 过来人经验×4，分类筛选 + 折叠展开）。
3. **v0.13**：全站打印样式；一键复制（资讯条目 / 对比文本 / 学校档案摘要）；更新日志弹窗。
4. **v0.14**：大字版无障碍切换（持久化）；「我的」本机数据导出 JSON / 一键清空。

验证：v0.11 47/47 · v0.12 50/50 · v0.13 54/54 · v0.14 57/57（Playwright 全量回归逐轮通过）。

## 零、v0.10 更新要点（2026-09-22）

1. **升学资讯中心（新栏目）**：五个分类（政策速递/升学动态/家庭教育/安全提醒/办事提醒）× 信息流 + 分类筛选；每条强制标注来源与日期；条目展开式阅读；空态齐备。
2. **站内采集与编辑台（新能力）**：「打开编辑台」→ 新增/编辑/删除条目，草稿→发布两态（草稿不入信息流）、必填校验、批量导入（行格式 `标题 | 来源 | 分类` 或 JSON）、导出 JSON 交接；内容保存在本机浏览器。
3. **采集脚本 collect.py（工程侧）**：种子数据 + `--fetch` 公开页面抓取（best-effort）→ 待审核队列（reviewed 标记）→ 审核后进数据文件；已实测抓取通道可用。
4. **门户级补齐**：新「关于与联系」栏目（定位/编辑审核规范/线索通道）；页脚升级为站点地图（13 链接）；顶部/移动导航新增资讯入口；skip link；JSON-LD 结构化数据；搜索管道合并资讯（命中定位展开）。
5. **四专家评估**：设计/美工/教育/家长四视角深度评审，本轮 21 项整改全部落位（见《四专家评估报告-v0.10.html》）。

## 零・补、v0.9 更新要点（2026-09-22 凌晨，模块化）

1. **模块化工程**：源码拆分为 `src/` 三层结构（styles 基础+18 个区块模块 / data.js 数据层 / features 20 个功能模块），新增 `build.py` 构建脚本（含空模块守卫、占位符残留检查、未登记模块拦截、幂等跳过）；**产物仍为单文件零依赖**，拆分经逐字节一致性验证（sha256 对齐）。
2. **首屏孩子快切条**：hero 下方常驻「当前关注」条——多孩一键切换、当前过滤状态一眼可见、无档案时引导去「我的」创建。
3. **同区学校徽标**：孩子档案所在区与学校卡匹配时，卡片描边 + 「就在 XX 区」徽标，扫一眼找到同区学校。
4. **自查器预填**：进入页面自动把入学自查的「区县/学段」预填为当前孩子档案（只填空态，不覆盖已选）。
5. **体验打磨**：全区块锚点偏移（scroll-margin-top，吸顶栏不再遮挡标题）、prefers-reduced-motion 动效偏好、og: 分享标签 + theme-color、页脚版本串同步。

## 零・补、工程结构（v0.9 起）

```text
DELIVERY/鸡父母平台-MVP网站/
├── index.html          # 构建产物（单文件，部署/双击即用）
├── build.py            # 构建脚本：python3 build.py
├── collect.py          # 资讯采集脚本：python3 collect.py [--fetch]
├── src/
│   ├── template.html   # HTML 骨架（含 /*__BUILD_CSS__*/ 与 //__BUILD_JS__ 占位符）
│   ├── styles/
│   │   ├── base.css    # 变量/重置/通用
│   │   └── modules/    # 27 个区块样式（01-top … 27-materials）
│   ├── data.js         # DATA_VERSION / DATA_SOURCES / SCHOOL_DB / SEARCH_DB / PATH_MAP
│   ├── data-news.js    # 资讯数据（由 collect.py 生成 · 勿手改）
│   ├── news/
│   │   ├── seed.json    # 资讯种子（编辑部维护）
│   │   ├── sources.json # 采集源清单（名称/URL/分类）
│   │   └── collected.json # 抓取缓存（reviewed 标记后发布）
│   └── features/       # 30 个功能模块（_order.txt 定序：30-news / 34-theme / 35-pageui / 36-a11y / 37-lifecalc 速算 / 38-countdown / 39-materials）
└── README.md
```

改版流程：改 `src/` 对应模块 → `python3 build.py` → 产物 index.html 自动更新 → 浏览器刷新即见。改文案去 10-demo-data.js，改配色去 base.css 变量区，改资讯去 `src/news/seed.json` 后跑 `python3 collect.py`，加功能新建 xx.js 并登记进 _order.txt。

按《重庆鸡父母平台企划书》V2.0 第五章（产品方案）+ 第四章（12 模块需求图谱）+ §7.5（经营流程）搭建的可交互演示站。**单文件、零依赖、双击即开。**

## 零、v0.8 更新要点（2026-09-21 晚）

1. **多孩档案**：「我的 → 孩子档案」支持添加多个孩子（称呼 + 学段 × 区县），点孩子条即切换「当前孩子」；旧单孩档案自动迁移，不丢数据。
2. **日历个性化**：按当前孩子的学段自动过滤节点，日历顶部显示「已按 XX 过滤」提示条，可一键查看全部学段。
3. **提醒导出 .ics**：「我的提醒」可一键导出标准日历文件，导入手机/电脑日历后本地提醒（正式版为微信服务通知 + 日历订阅双通道）。
4. **行动清单打印**：日历区「打印行动清单」按当前过滤结果生成 A4 打印页（含当前孩子信息与各节点「现在该做什么」），可直接打印或另存 PDF。
5. 全站搜索新增 v0.8 条目；页脚版本记录同步。

## 一、运行方式

- **最简单**：双击 `index.html`（Chrome/Edge/Safari 均可，无需服务器）
- **本地服务**（可选）：在本目录执行 `python3 -m http.server 8765`，访问 `http://localhost:8765/index.html`
- 移动端：手机浏览器打开同一文件，已做响应式适配（iOS/Android 均无横向溢出）

## 二、已实现模块（v0.7 全量 · 对照企划书）

| # | 页面模块 | 对应企划书 | 交互能力 |
|---|---|---|---|
| 0 | 三入口首屏 | §5.1 三入口原则 | 找政策/找学校/问问题，锚点直达 |
| 0.2 | **数据来源与核验面板**（v0.7 新） | 附录「数据有据可查」 | DATA_SOURCES 注册表（9 项真实数据，全部已核验）自动生成来源表：数据点/来源/截至/核验状态；首屏数据版本徽标（real-2026-09）；志愿/资金/自查工具输出均挂「✓已核验+来源+截至」徽标 |
| 0.5 | **全站搜索**（v0.3 新） | §5.4 页面 #2 | 跨政策/学校/术语/求真/日历即时检索；命中跳转+高亮闪动；空态提示；回车首条、Esc 关闭 |
| 1 | 升学日历 | §4.2.4 | 5 条节点（含幼升小，覆盖 4 学段）+ **一键设提醒** + **「现在该做什么」行动清单折叠**（每卡 3 条可执行项） |
| 2 | 政策库 + 术语词典 | §4.2.1 | 原文与人话版对照；**版本历史折叠**（2026-04→06 更新轨迹） |
| 3 | 择校对比器 | §4.2.3 | 选 2–3 校生成 9 维对比；重复去重、空态、单选校验；**不提供录取线（合规红线）**；**收藏对比**可恢复；**差异标记 + 仅看差异**（v0.18） |
| 3.2 | **学校档案库**（v0.3 新） | §4.2.2 | 12 所样例学校卡；**档案弹窗**（9 字段+数据来源+核验日期+暂缺字段如实标注）；**审核制评价**（已审核/争议仲裁徽标）；一键加入对比 |
| 3.5 | 志愿位次换算 | §4.2.5 | 线差法演示（2026 特招线 510/496）；非法输入校验；标注口径局限 |
| 3.6 | **升学路径地图**（v0.6 新） | P2 整改 · 升学路径模块 | 6 条路径（普高/指标到校/民办/中职职教/艺体/出国）× 4 学段时间轴 × 风险提示，tab 切换 |
| 4 | 求真辟谣台 + 线索提交 | §4.3 | 4 条分级结论 + 线索表单（≥10 字校验→编号 FQ-） |
| 4.5 | 家长社区 | §4.2.6 | 4 条示例帖（精华/讨论中/实操/归档）+ 变现挂点说明 |
| 5 | 生活服务五卡 + 租房行情表 | §4.2.8–12 | 3 校周边行情（挂牌≠成交口径、样本量、核验月份） |
| 5.5 | 升学豆中心 | §6.3 | 签到 +5（防重复）；**与「我的」页余额实时同步** |
| 5.6 | 教育资金规划 | §4.2.12 | 学段+预算 → CIEFR 均值对照 + 分学段建议（实时联动） |
| 5.7 | **我的**（v0.3 新 · v0.8 增强） | §7.5 档案完善 | **多孩档案**（v0.8：多孩子切换、自动迁移旧档案）；**我的提醒**（日历设提醒在此管理/删除，v0.8 支持导出 .ics）；**对比收藏**（一键恢复到对比器） |
| 5.8 | **B 端合作**（v0.3 新） | §6.6 | 三类合作形态说明 + 意向登记（校验→编号 BZ-，对应资质预审 SOP） |
| 5.9 | **陪读成本速算器**（v0.22 新） | §4.2.8-12 | 片区 × 房型 × 生活档位 × 通勤 → 月度开销区间；一键复制（样本口径） |
| 5.10 | **入学材料清单生成器**（v0.25 新） | §4.2.5 | 学段 × 户籍 × 住房 → 分组材料清单；复制 / 打印（演示口径） |
| 5.11 | **倒计时 / 数据互通**（v0.23–v0.24 新） | §4.2 | 首屏「距下一节点」提示；本机数据导入恢复 + 统计条 |
| 6 | 会员体系 + **开通演示流**（v0.3 增强） | §6.2 表 6-3 | 四档权益矩阵；**协议勾选校验→订单生成（DD-）→≤2h SLA 履约演示→赠豆**；自动续费默认关闭 |
| 7 | 履约承诺流程 | §7.5 | 六环节 SLA 卡片（48h/2h/24h/12h） |

## 三、修改指南（改哪里）

所有内容都在 `index.html` 内：

- **学校数据**：搜 `SCHOOL_DB`（JS 对象；含简介/评价数组，新增学校会自动出现在档案库与对比下拉）
- **搜索索引**：搜 `SEARCH_DB`（新增模块时把条目加进去即可被全站搜索命中）
- **日历节点**：搜 `id="calendar"` 下的 `.cal` 卡片（含 `data-title` 提醒关键词）
- **政策条目与版本历史**：搜 `id="policy"` 下的 `.pol-item` 与 `details.vh`
- **行情表**：搜 `rent-table`
- **会员权益**：搜 `id="plans"`（价格在 `.price`；开通按钮绑 `openOrder(名称, 价格, 赠豆)`）
- **配色**：文件头 `:root` CSS 变量（--navy 主色 / --accent 强调色）
- **换算逻辑**：`var ZY`（特招线基准）与 `runZy()`；资金规划在 `fundCalc()`
- **本地存储键**：`jfm_children`（多孩档案）/ `jfm_active`（当前孩子）/ `jfm_alerts`（提醒）/ `jfm_cmp_fav`（对比收藏）/ `jfm_news_user`（资讯编辑）/ `jfm_theme`（外观）/ `jfm_fs`（大字版）/ `jfm_ann_closed`（公告条）；旧 `jfm_profile` 自动迁移
- **构建**：`python3 build.py`（v0.9 起，源码在 src/，产物单文件）

## 四、质量说明（v0.3）

- 桌面/移动双端响应式（960/620px 断点全覆盖含 v0.3 新模块）；移动端全页滚动无溢出（实测 0px）
- 空态/错误态齐备：搜索无结果、对比器空态与单选提示、档案未保存态、提醒空态、协议未勾拦截、B 端/求真字数校验
- 交互状态：hover 抬升、按钮 hover/禁用、Toast 反馈（2.2s）、锚点平滑滚动+闪动高亮、native `<dialog>` 弹窗（焦点管理）、`aria-label`/`aria-live`/`:focus-visible`
- **Playwright 自动化验证 25/25 用例通过**（2026-09-16）：搜索 4 项、学校档案 4 项、提醒 2 项、档案/收藏 2 项、开通流 3 项、B 端 2 项、豆同步 1 项、旧功能回归 2 项、移动端 1 项、零报错 1 项
- 全站零外部依赖（无远程字体/图片/脚本），断网可开

## 五、合规内置

不承诺升学结果 · 不做学科培训 · 不提供录取线与排名（对比器内说明）· 机构「资质审核 + 平台不背书」· 房源信息服务平台口径（不居间、不代收租金、费与成交脱钩）· 升学豆不可提现 · 会员协议勾选+退款规则明示+自动续费默认关闭 · 求真结论逐条带出处与核验日期 · 全国口径数据标注需本地校验 · 「清朗巴渝」红线已纳入审核词库（页脚声明）。

## 六、后续扩展方向

1. 接真实数据：政策库→CMS、学校档案→官方公示字段抓取、行情表→后台台账（SOP 执行包配套）
2. 账号与支付：注册/登录/支付在小程序侧承载（§5.4 26 页面清单），本站作为展示与验证层
3. 日历订阅：微信服务通知 + .ics 双通道；提醒升级为跨设备
4. 运营看板：接入每日三张表（收入/成本/漏斗）的只读视图

## 七、验证记录（v0.26 全量 · 88/88）
本批四轮逐轮全量回归：v0.23 82/82 → v0.24 84/84 → v0.25 86/86 → v0.26 88/88（Playwright 真实浏览器）。覆盖：数据导入、倒计时、材料清单、键盘导航、学校筛选等新增能力。

## 七、验证记录（v0.22 全量 · 80/80）
本批四轮逐轮全量回归：v0.19 72/72 → v0.20 75/75 → v0.21 78/78 → v0.22 80/80（Playwright 真实浏览器）。

## 七、验证记录（v0.10 · 44/44）
29 项 v0.9 全量回归 + 15 项 v0.10 新增：资讯渲染 12 条 / 分类筛选 / 展开收起 / 编辑台必填校验 / 新增发布入流 / 草稿两态 / 编辑 / 批量导入 / 导出 JSON / 删除同步 / 搜索定位展开 / skip link / JSON-LD / 关于与站点地图 / 资讯上下文零报错。

## 七・补、v0.9 验证记录（29/29，2026-09-22 凌晨）

## 八、数据接入说明（v0.7 新）

**当前数据包**：`real-2026-09`（9/9 项已核验）。数据源注册表在 `index.html` 的 `DATA_SOURCES`（JS 对象），每项含 label/source/asOf/verified 四要素。接入新数据的步骤：

1. 在 `DATA_SOURCES` 追加或更新条目（来源四要素 + 核验状态）
2. 更新 `DATA_VERSION`（id/label/asOf）
3. 页面数字与 `dataBadge(key)` 徽标自动联动；「数据来源与核验」面板自动重渲染

**真实性验证记录（2026-09-17）**：志愿换算输出真实 2026 特招线（496/510，新华网重庆核验）+已核验徽标；资金规划输出 CIEFR 真实均值 15,828 元对照（30,000 元=190%）+报告名徽标；入学自查输出 70% 指标到校口径（渝教基函〔2025〕21号）+徽标；来源面板 9/9 全部「✓已核验」。排查记录：测试中发现 8765 端口被陈旧 http.server 占用导致回归误读旧页面，清除端口后 curl 预检+全量复测通过——这也验证了「验证环境要先核验」的纪律。

## 九、版本记录

- v0.1（2026-09-16 下午）：首版 6 模块（三入口/日历/政策/对比/求真/生活+会员+流程）
- v0.2（晚间）：+6 模块（志愿/社区/升学豆/资金/求真提交/租房行情），16/16 验证
- v0.3（晚间）：+全站搜索/学校档案库/我的/会员开通演示/B端合作；修复 toast 缺失、移动端溢出、豆余额同步；**25/25 验证通过**
- v0.4（次日晨）：三专家评审先行整改——入学自查器/信任指标条/新手引导/汉堡导航/路径 6 卡/求真筛选/热议榜/返回顶部/favicon；修复汉堡按钮嵌套与 227px 溢出
- v0.5（次日）：三专家评审 12 条合并（经营语言清出家长界面、年份标注、2–3 所口径统一、学段筛选、B端移位、术语 13 条、录取线互链）；修复筛选器类名错位
- v0.6（2026-09-17 晨）：**升学路径地图**（6 路径×4 学段时间轴×风险提示）+ **日历「现在该做什么」行动清单**×5（评审 P2 项落地）；**20/20 验证通过**
- v0.7（2026-09-17）：**数据层**——DATA_SOURCES 注册表（9 项真实已核验数据）驱动「数据来源与核验」面板与全站徽标；志愿换算接真实 2026 特招线、资金规划接 CIEFR 真实均值、自查器接 70% 指标口径；修复陈旧端口进程导致的测试环境污染；**16/16 验证通过**
- v0.8（2026-09-22 凌晨）：**多孩与个性化**——多孩档案（切换/迁移）、日历按孩子学段自动过滤、提醒导出 .ics、行动清单 A4 打印；全站搜索与页脚版本同步；**22/22 验证通过**
- v0.9（2026-09-22 凌晨）：**模块化工程 + 针对性增强**——src/ 三层拆分（styles 19 段/data/features 20 模块）+ build.py 构建链（逐字节一致性验证）；首屏孩子快切条、同区学校徽标、自查器预填、锚点偏移、动效偏好、分享标签；**29/29 验证通过**
- v0.29（当前）：**多页架构改版**——17 页真跳转 + 跨页参数联动 + 旧锚点重定向；**24/24 验证通过**
- v0.28：**链接真实化与内容充实**——数据来源 9 条官方入口、资讯 30 条（4 条带原文链接）、搜索索引含正文；**94/94 验证通过**
- v0.27：**全站审查优化**——打印隐藏交互控件、导出补全键、公告条与搜索占位更新；**91/91 验证通过**
- v0.26：**体验优化**——数据导入恢复与统计条、关键节点倒计时条、入学材料清单生成器、搜索键盘导航与学校库筛选；**88/88 验证通过**
- v0.25：入学材料清单生成器（学段 × 户籍 × 住房 → 清单）；**86/86 验证通过**
- v0.24：关键节点倒计时条（首屏今日提示）；**84/84 验证通过**
- v0.23：本机数据导入恢复 + 「我的」数据统计条；**82/82 验证通过**
- v0.22：**内容与工具扩充**——学校档案库扩至 12 所 + 搜索直达；政策库 / 术语；学堂 / 求真 / 社区 / 资讯扩容；陪读成本速算器；**80/80 验证通过**
- v0.21：家长学堂 / 求真台 / 社区 / 资讯扩充（至 26 条）；**78/78 验证通过**
- v0.20：政策库 +4 条、术语词典扩至 18 条、搜索索引同步；**75/75 验证通过**
- v0.19：学校档案库扩容至 12 所、搜索直达学校档案；**72/72 验证通过**
- v0.18：**第二批四轮迭代**——深色模式、资讯快搜、键盘与无障碍、对比差异标记；**69/69 验证通过**
- v0.17：键盘与无障碍（焦点描边 / 「?」快捷键帮助 / 弹窗焦点圈定 / aria 状态）；**66/66 验证通过**
- v0.16：资讯快搜（标题/摘要/来源）+ 返回顶部 + 阅读进度条；**63/63 验证通过**
- v0.15：深色模式（跟随系统 / 手动切换，本机记忆）；**60/60 验证通过**
- v0.14：**大字版与全量细节**——大字版无障碍、本机数据管理、打印与一键复制、家长学堂、资讯扩容；四轮迭代逐轮全量回归（47/50/54/57 项）；**57/57 验证通过**
- v0.13：打印优化 / 一键复制分享（资讯·对比·档案）/ 更新日志弹窗；**54/54 验证通过**
- v0.12：家长学堂（专家问答 + 过来人经验）；**50/50 验证通过**
- v0.11：资讯扩容 20 条 / 公告条 / 搜索快捷键 / 导航高亮；**47/47 验证通过**
- v0.10：**资讯中心与站内采集编辑**——资讯信息流（5 分类/筛选/展开/空态）、编辑台（新增/编辑/草稿/导入导出）、collect.py 采集脚本（种子+抓取+待审队列）；门户补齐（关于与联系/站点地图/skip link/JSON-LD/搜索集成）；四专家评估 21 项整改；**44/44 验证通过**
